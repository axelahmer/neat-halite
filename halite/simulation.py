from kaggle_environments import evaluate, make
import numpy as np

a = evaluate(
    "halite",
    ["random", "random", "random", "random"],
    num_episodes=6
)


def run_swiss_tournament(players: [], rounds: int) -> []:
    scores  = np.zeros(len(players), rounds)
    for round in rounds:

    return scores


print(a)


class Simulation(object):
    def __init__(self):
        pass
